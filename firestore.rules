rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // User history subcollection rules
    match /users/{userId}/history/{documentId} {
      allow read, write, delete: if request.auth != null && request.auth.uid == userId;
    }
  }
}
